export function ResponsiveSection() {
  return (
    <p className="text-sm text-fg-muted">
      Full responsive / accessibility walkthrough (360 / 768 / 1280 px, axe sweep) lands in CP3 (
      <code className="text-xs">D3.5</code>).
    </p>
  );
}
